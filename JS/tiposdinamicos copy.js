// tipagem fraca no js

var tipagem = true;

// para ver qual o tipo de varialve, usamos:

console.log(typeof tipagem);

//podemos mudar que tipo de variavel é, apenas trocando o valor.

tipagem = "agora é uma string";

console.log(typeof tipagem);

// não podemos fazer isso quando é declarado uma const, isso sabemos pois vimos na ultima aula
