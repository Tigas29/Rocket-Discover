const sum = function () {
  let teste = 1;
  let test = 2;
  var cal = teste + test;
  return cal;
};

sum();

let Div = function (a, b) {
  // a = 2;
  // b = 3;
  return a / b;
};

console.log(Div());

// /podemos colocar o valor dentro da chamada de function

console.log(Div(10, 22)); //o que estamos passando aqui dentor são argumentos;
