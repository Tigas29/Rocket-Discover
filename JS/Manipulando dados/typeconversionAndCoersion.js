// type coversion faz com que  eu altera de forma implicita um tipo de dado.
console.log("5" + 2);

// tranformas os dois para uma string para que não ocorra nenhum problemas.

// caso queiramos mudar a string para numero, podemos fazer isso.

console.log(Number("2") + 2);

// agora ele vai fazer uma soma. e isso se chama type conversion , pois fomos nós que fizemos a manipulaçõa
